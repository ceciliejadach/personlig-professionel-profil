---
import vildMad_1 from "../assets/images/vild-mad-1.png";
import vildMad_2 from "../assets/images/vild-mad-2.png";

const { text, link, paragraph } = Astro.props;
---

<div class="container">
  <div class="card-wrapper">
    <div class="card">
      <div class="content">
        <h1>Vild Mad - Sanke App</h1>
        <div>
          <ul>
            <li>&#8594 Developer</li>
            <li>&#8594 Digital Designer</li>
            <li>&#8594 UX Designer</li>
          </ul>
        </div>
        <p><strong>Løsning:</strong> <a href="https://www.figma.com/design/UG3ISjU0LuEENJSu2VUa8q/VILD-MAD?node-id=176-1888&node-type=canvas&t=Zzfougr5vD4SnKIw-0">Figma Prototype</a></p>
      </div>
      <figure>
        <img src={vildMad_1.src} alt="" />
        <img src={vildMad_2.src} alt="" />
      </figure>
    </div>
  </div>
</div>

<style>
  @layer components {
    ul {
      padding: 0;
      li {
        list-style: none;
        font-size: 1.15rem;
      }
    }
    img {
      border-radius: 10px;
      box-shadow: rgba(99, 99, 99, 0.2) 0px 2px 8px 0px;
    }
    .container {
      padding-block: 3rem;
    }
    .card-wrapper {
      container-type: inline-size;
      /* padding: 1rem; */
      grid-column: full;
    }

    .content {
      display: grid;
      align-self: start;

      div {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
      }
    }

    figure {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      position: relative;

      &::before {
        content: "";
        position: absolute;
        width: 150px;
        height: 300px;
        border-radius: 10px;
        border: 2px dashed var(--blue);
        top: -2%;
        left: -2%;
        z-index: -10;
      }
    }

    .card:has(img) {
      display: grid;
      gap: 1rem;

      @container (width > 800px) {
        grid-template-columns: 3fr 2fr;

        .card-wrapper {
          display: grid;
        }

        .content {
          order: 1;
        }
      }
    }

    a {
      text-decoration: none;
      color: var(--blue);
      padding-right: 30px;
      position: relative;

      &::after {
        content: "";
        position: absolute;
        height: 0.7px;
        background: var(--blue);
        left: 0;
        bottom: 0;
        width: 0;
        transition: width 0.2s;
      }

      &:hover:after {
        width: 80%;
      }
    }

    @media (width > 600px) {
      .container {
        padding-block: 6rem;
      }
      .content {
        gap: 1rem;
      }
      .card:has(img) {
        gap: 3rem;
      }
    }
  }
</style>
